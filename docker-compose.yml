version: '3'

services:
  db:
    image: "postgres:latest"
    container_name: planthub-db
    ports:
      - "5420:5433"
    volumes:
      - dbdata:/var/lib/postgresql/data
    env_file:
      - "docker.env"
    networks:
      - dbnet
volumes:
  dbdata:
networks:
  dbnet:
    driver: host